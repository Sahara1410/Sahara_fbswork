#include<stdio.h>
#include<string.h>
void main()
{
	char str[]="Sahara Pathan";
	printf("The string is:%s",str);
	int count=0;
	int i;
	for(i=0;i<=str[i];i++)
	{
		count++;
	}
	printf("\nTotal no in string is:%d",count);
}